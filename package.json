{
  "name": "ticket-management-system",
  "version": "1.0.0",
  "description": "SPA para gestión de tickets de soporte técnico",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "auth-server": "json-server --watch auth-db.json --port 3001",
    "data-server": "json-server --watch data-db.json --port 3002"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "sass": "^1.100.0",
    "vite": "^8.0.16"
  },
  "dependencies": {
    "axios": "^1.16.1"
  }
}